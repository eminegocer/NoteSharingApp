<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="A modern and elegant note-sharing app" />
    <meta name="author" content="Your Name" />
    <link rel="icon" type="image/x-icon" href="wwwroot/favicon.ico" />

    <!-- Font Awesome icons -->
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/css/style.css" asp-append-version="true" />

    <style>
        /* Sayfanın tamamını kaplayan arka plan */
        .full-page-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('/img/home-bg.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            z-index: -1;
        }

        /* İçerik kutusu */
        .content {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }

        /* Kart tasarımı */
        .card-container {
            background: rgba(255, 255, 255, 0.1); /* Şeffaf beyaz kutu */
            backdrop-filter: blur(8px); /* Arka planı bulanıklaştır */
            padding: 60px; /* Padding değerini artırdık */
            border-radius: 15px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.2);
            color: white;
            max-width: 500px;
            margin: auto;
        }

            .card-container p {
                margin-bottom: 15px; /* Yazılar arasına boşluk ekledik */
                color: #9c7ed3;
                

            }

        .btn-custom {
            background-color: #f4ebfe;
            color: #7a5fc0;
            font-weight: bold;
            padding: 12px 24px;
            border-radius: 25px;
            transition: all 0.3s ease-in-out;
            margin-top: 30px;
        }

        .btn-custom:hover {
            background-color: #e1d0fa;
            color: #7a5fc0;
        }


    </style>

</head>
<body>
    <div class="full-page-background"></div>

    <div class="content">
        <div class="card-container">
            <h1 style="color:#9c7ed3;"> Note Sharing App'a Hoş Geldiniz </h1>
            <p>Notlarını diğer kişilerle kolayca paylaş ve değerli içerikleri keşfet.</p>
            <p>Notlara göz atın, katkıda bulunun ve bilginizi düzenleyin.</p>
            <p>Başlamak için butona tıklayın!</p>
            <div>
                <a class="btn btn-custom" asp-controller="Home" asp-action="Login">Get Started</a>
            </div>
        </div>
    </div>

</body>
</html>
